(() => {'use strict';const input=document.getElementById('busca'),status=document.getElementById('searchStatus'),items=[...document.querySelectorAll('.searchable')];const normalize=s=>s.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();function filter(){const q=normalize(input.value.trim());let n=0;items.forEach(el=>{const match=!q||normalize(el.textContent).includes(q);el.hidden=!match;if(match)n++;if(el.tagName==='DETAILS')el.open=Boolean(q&&match);});status.hidden=!q;status.textContent=n?`${n} tópicos encontrados. Veja abaixo.`:'Nenhum tópico encontrado. Tente outra palavra ou apague a busca.';}input.addEventListener('input',filter);document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',()=>{if(input.value){input.value='';filter();}}));})();