name: disabled legacy one-off cafe dark card runtime fix

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled legacy one-off workflow. Kept only for audit history; do not run for deploys."
